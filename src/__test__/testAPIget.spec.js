const getAPI = require('../server/getAPI.js');

describe("Testing the API get functionality", () => { 
    test("Testing the getAPI() function", () => {
        expect(getAPI).toBeDefined();
})});